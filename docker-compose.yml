services:
  agrisage:
    build:
      context: .
      dockerfile: AgriSage.API/Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=server=host;user=user;password=pass;database=database;